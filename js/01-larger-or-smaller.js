//Larger or Smaller?

let firstInteger = parseInt(prompt('Enter any integer'), 10);
let secondInteger = parseInt(prompt('Enter any integer'), 10);
if (firstInteger >= secondInteger) {
    document.write(firstInteger);
} else {
    document.write(secondInteger);
}